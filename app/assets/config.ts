// const hostBackend = import.meta.env.VITE_HOST_BACKEND || import.meta.env.NUXT_PUBLIC_HOST_BACKEND || ''

// export const hostBackend = hostBackend || (typeof window !== 'undefined' ? window.location.origin : '')
export const hostBackend = 'http://localhost:3000'